void setup()
{
  // Configura el pin 13 como salida (OUTPUT)
  pinMode(13, OUTPUT);
}

void loop()
{
  // Establece el pin 13 a estado HIGH (encendido), lo que encenderá el LED.
  digitalWrite(13, HIGH);
}

